@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --ice: #dff4ff;
  --snow: #f8fdff;
  --glow: #85e3ff;
  --deepBlue: #6aa8ff;
  --frost: #c8f1ff;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  padding: 0;

  background:
    radial-gradient(circle at top,
      #dff7ff 0%,
      #a9e7ff 35%,
      #79c8ff 100%);

  min-height: 100vh;
  overflow-x: hidden;

  color: white;
  font-family: Arial, Helvetica, sans-serif;
}

.glass {
  background: rgba(255,255,255,0.12);
  backdrop-filter: blur(24px);
  border: 1px solid rgba(255,255,255,0.15);
}

.ice-glow {
  box-shadow:
    0 0 20px rgba(133,227,255,0.5),
    0 0 50px rgba(133,227,255,0.25);
}
